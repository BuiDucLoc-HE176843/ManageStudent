@{
    ViewData["Title"] = "Add Teacher";
}

<style>
    .form-container {
        max-width: 500px;
        margin: 50px auto;
        padding: 20px;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        border: 1px solid #ddd;
    }
</style>

<div class="container">
    <div class="form-container">
        <h2 class="text-center">Add Teacher</h2>
        <form asp-action="AddTeacher" method="post">
            @Html.AntiForgeryToken() 

            <div class="mb-3">
                <label class="form-label">Teacher Name</label>
                <input name="Name" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Code</label>
                <input name="Code" class="form-control">
            </div>

            <div class="mb-3">
                <label class="form-label">Email</label>
                <input name="Email" type="email" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Phone</label>
                <input name="Phone" class="form-control" required pattern="\d{10}" placeholder="Enter 10-digit phone number">
            </div>

            <div class="mb-3">
                <label class="form-label">Date of Birth</label>
                <input name="Dob" class="form-control" type="date" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Address</label>
                <input name="Address" class="form-control">
            </div>

            <button type="submit" class="btn btn-primary w-100">Add Teacher</button>
            <a asp-action="ManageTeachers" class="btn btn-secondary w-100 mt-2">Cancel</a>
        </form>
    </div>
</div>
